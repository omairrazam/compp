<tr>
  <td>
    <%#= link_to item.photo, :action => "show",
                  :controller => "photos", :id => item.photo.id %>
    <%= link_to item.photo do %>
      <%= image_tag item.photo.image(:mini) %>
    <% end %>
	</td>
  <td></td>
  <td><input type="number" name="qty" min="1" value = <%= item.qty %> ></td>
  <td>$<%= sprintf("%0.2f", item.unit_price * item.qty) %></td>
  <td>$<%= sprintf("%0.2f", item.unit_price * item.qty) %></td>
  <td>
    <%= link_to "remove",carts_remove_path(:id => item.photo.id), method: :post,data: {confirm: 'Are you sure'}, class: "btn btn-danger btn-xs"%>
  </td>
</tr>