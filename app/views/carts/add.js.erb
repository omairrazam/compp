$("#cart").html("<%= escape_javascript(render 'carts/partials/cart_nav') %>")
$("#photo_<%= @photo_id %> .photo_cart_div").html('<strong><%= escape_javascript( link_to "Remove from cart", carts_remove_path(:id => @photo_id),remote: true, method: :post,data: {confirm: "Are you sure"})%></strong>')